import { MessageLogsView } from "@/components/reports/message-logs-view";

export const metadata = {
    title: "Messages Log Reports | WhatsApp Server",
};

export default function MessageReportsPage() {
    return <MessageLogsView />;
}
